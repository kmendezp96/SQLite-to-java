# SQLite-to-java
# Se debe instalar el siguiente plugin http://www.eclipse.org/windowbuilder/download.php para eclipse
# Adicionalmente tambien se tiene que tener la libreria de Antlr en la ruta C:\Javalib
# Finalmente en SQLiteBaseVistors 1127, 936, 858, 635, 550 cambiar las rutas por la de donde aloje el proyecto/src/translate esto para tener los archivos generados por el programa en el mismo proyecto.
# Se corre desde MWindow.java para interfaz grafica se le da el archivo de entrada y nos mostrara los logs
# Si la entrada tiene consultas estos se mostrataran en la consola de eclipse
# Tambien se puede correr desde el archivo prueba.java sin interfaz grafica, este nos generara las clases pero no los logs y su archivo de entrada será el input.txt en la carpeta del proyecto
